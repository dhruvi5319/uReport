---
phase: 07-react-design-system-and-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.ts
  - frontend/postcss.config.js
  - frontend/index.html
  - frontend/src/globals.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/lib/utils.ts
  - frontend/src/lib/animations.ts
  - frontend/src/lib/api.ts
  - frontend/src/contexts/ThemeContext.tsx
  - frontend/src/contexts/AuthContext.tsx
  - frontend/src/pages/LoginPage.tsx
  - frontend/src/pages/DashboardPage.tsx
  - frontend/src/components/shell/AppShell.tsx
  - frontend/src/__tests__/theme.test.tsx
  - frontend/src/__tests__/tokens.test.ts
autonomous: true

features:
  implements: ["F17", "RESP-01"]
  depends_on: []
  enables: ["F18", "F19", "ANIM-01"]

must_haves:
  truths:
    - "globals.css contains all CSS custom property definitions for both :root (light) and .dark (dark mode)"
    - "ThemeProvider reads localStorage 'ureport-theme' and applies/removes .dark class on <html>"
    - "animations.ts exports all 4 variants (pageVariants, containerVariants, itemVariants, stepVariants) with durations ≤ 0.3s"
    - "vite.config.ts binds dev server to 0.0.0.0:5173 with proxy to localhost:8080"
    - "npm run build exits 0 with no TypeScript errors"
    - "AuthContext calls GET /api/auth/me on mount and stores user.role for nav filtering"
  artifacts:
    - path: "frontend/src/globals.css"
      provides: "Design token CSS custom properties (both :root and .dark)"
      contains: "--color-primary"
    - path: "frontend/src/lib/animations.ts"
      provides: "Framer Motion animation variant presets"
      exports: ["pageVariants", "containerVariants", "itemVariants", "stepVariants"]
    - path: "frontend/src/contexts/ThemeContext.tsx"
      provides: "ThemeProvider + useTheme hook"
      exports: ["ThemeProvider", "useTheme"]
    - path: "frontend/src/contexts/AuthContext.tsx"
      provides: "AuthProvider + useAuth hook"
      exports: ["AuthProvider", "useAuth"]
    - path: "frontend/tailwind.config.ts"
      provides: "Tailwind config extending CSS variables as colors"
      contains: "hsl(var(--color-primary))"
  key_links:
    - from: "frontend/src/contexts/ThemeContext.tsx"
      to: "document.documentElement.classList"
      via: "toggles .dark class in useEffect"
      pattern: "classList\\.(add|remove|toggle).*dark"
    - from: "frontend/src/contexts/AuthContext.tsx"
      to: "/api/auth/me"
      via: "axios GET on mount"
      pattern: "get.*api/auth/me"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/contexts/ThemeContext.tsx"
      via: "ThemeProvider wrapping all routes"
      pattern: "ThemeProvider"

integration_contracts:
  requires: []
  provides:
    - artifact: "frontend/src/lib/animations.ts"
      exports: ["pageVariants", "containerVariants", "itemVariants", "stepVariants"]
      shape: |
        export const pageVariants = { initial, animate, exit };
        export const containerVariants = { animate: { transition: { staggerChildren: 0.05 } } };
        export const itemVariants = { initial, animate };
        export const stepVariants = { enter(dir), center, exit(dir) };
      verify: "grep -n 'export const pageVariants' frontend/src/lib/animations.ts && grep -n 'export const stepVariants' frontend/src/lib/animations.ts && echo CONTRACT_OK"
    - artifact: "frontend/src/contexts/ThemeContext.tsx"
      exports: ["ThemeProvider", "useTheme", "Theme"]
      shape: |
        export type Theme = 'light' | 'dark' | 'system';
        export function ThemeProvider({ children }: { children: React.ReactNode }): JSX.Element;
        export function useTheme(): { theme: Theme; setTheme: (t: Theme) => void; resolvedTheme: 'light' | 'dark' };
      verify: "grep -n 'export function ThemeProvider' frontend/src/contexts/ThemeContext.tsx && grep -n 'export function useTheme' frontend/src/contexts/ThemeContext.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/contexts/AuthContext.tsx"
      exports: ["AuthProvider", "useAuth", "User"]
      shape: |
        export interface User { personId: number; username: string; role: 'staff' | 'admin'; firstname: string; lastname: string; }
        export function AuthProvider({ children }: { children: React.ReactNode }): JSX.Element;
        export function useAuth(): { user: User | null; loading: boolean; logout: () => Promise<void> };
      verify: "grep -n 'export function AuthProvider' frontend/src/contexts/AuthContext.tsx && grep -n 'export function useAuth' frontend/src/contexts/AuthContext.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/lib/utils.ts"
      exports: ["cn"]
      shape: "export function cn(...inputs: ClassValue[]): string"
      verify: "grep -n 'export function cn' frontend/src/lib/utils.ts && echo CONTRACT_OK"
    - artifact: "frontend/src/globals.css"
      exports: ["--color-primary", "--color-background", "--color-foreground", "--color-status-open"]
      shape: ":root { --color-primary: 221 83% 53%; ... } .dark { --color-background: 222 47% 11%; ... }"
      verify: "grep -n 'color-primary' frontend/src/globals.css && grep -n '\\.dark' frontend/src/globals.css && echo CONTRACT_OK"
    - artifact: "frontend/tailwind.config.ts"
      exports: ["colors.primary", "colors.status", "fontFamily.sans", "fontFamily.mono"]
      shape: "extend.colors.primary.DEFAULT = 'hsl(var(--color-primary))'"
      verify: "grep -n 'hsl(var(--color-primary))' frontend/tailwind.config.ts && echo CONTRACT_OK"
---

<objective>
Scaffold the complete React + Vite + TypeScript project foundation for the uReport CRM frontend, including the full design token system (Tailwind CSS config + CSS custom properties), Framer Motion animation presets, ThemeContext (dark mode), and AuthContext (JWT session management).

Purpose: Every subsequent frontend plan builds on top of these tokens, contexts, and utilities. Without them no UI component can be styled or authenticated correctly.
Output: Working Vite project that builds successfully; complete CSS token set for light + dark mode; animation presets; theme toggle that persists to localStorage; auth context that calls /api/auth/me.
</objective>

<feature_dependencies>
Implements: F17: Design system — Tailwind CSS with custom design tokens, CSS variables for civic blue, neutral gray, semantic status colors, dark mode palette; Inter for UI text, JetBrains Mono for IDs/code; 4px base grid; 3-tier shadow system; consistent border radii. RESP-01: Every screen works at 375px, 768px, 1280px+; mobile-first CSS.
Depends on: None
Enables: F18: Navigation shell (needs ThemeContext, AuthContext, design tokens). F19: Accessibility (needs tokens for correct color contrast). ANIM-01: Framer Motion animations (needs animations.ts presets).
</feature_dependencies>

<execution_context>
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/workflows/execute-plan.md
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TypeScript project with design tokens</name>
  <files>
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tailwind.config.ts
    frontend/postcss.config.js
    frontend/index.html
    frontend/src/globals.css
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/lib/utils.ts
    frontend/src/lib/animations.ts
    frontend/src/lib/api.ts
    frontend/src/pages/LoginPage.tsx
    frontend/src/pages/DashboardPage.tsx
    frontend/src/components/shell/AppShell.tsx
  </files>
  <action>
Create the complete React + Vite + TypeScript project in the `frontend/` directory. The `frontend/Dockerfile` and `frontend/nginx.conf` already exist — do NOT modify them in this task (they will be updated in 07-04).

**Step 1 — Write `frontend/package.json`** with exactly these dependencies (use versions as specified; do NOT upgrade without reason):

```json
{
  "name": "ureport-crm",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-router-dom": "^6.26.0",
    "framer-motion": "^11.0.0",
    "@fontsource/inter": "^5.0.0",
    "@fontsource/jetbrains-mono": "^5.0.0",
    "clsx": "^2.1.0",
    "tailwind-merge": "^2.3.0",
    "lucide-react": "^0.400.0",
    "axios": "^1.7.0",
    "@radix-ui/react-dialog": "^1.1.0",
    "@radix-ui/react-popover": "^1.1.0",
    "@radix-ui/react-select": "^2.1.0",
    "@radix-ui/react-toast": "^1.2.0",
    "@radix-ui/react-tabs": "^1.1.0",
    "@radix-ui/react-avatar": "^1.1.0",
    "@radix-ui/react-alert-dialog": "^1.1.0",
    "cmdk": "^1.0.0",
    "class-variance-authority": "^0.7.0"
  },
  "devDependencies": {
    "vite": "^5.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "tailwindcss": "^3.4.0",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@vitest/ui": "^2.0.0",
    "@testing-library/react": "^16.0.0",
    "@testing-library/jest-dom": "^6.4.0",
    "jest-axe": "^9.0.0",
    "jsdom": "^24.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0"
  }
}
```

**Step 2 — Write `frontend/vite.config.ts`** (bind to 0.0.0.0 for sandbox iframe preview; NO X-Frame-Options):

```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

export default defineConfig({
  plugins: [react()],
  server: {
    host: "0.0.0.0",
    port: 5173,
    proxy: {
      "/api": { target: "http://localhost:8080", changeOrigin: true },
      "/open311": { target: "http://localhost:8080", changeOrigin: true },
      "/auth": { target: "http://localhost:8080", changeOrigin: true },
    },
  },
  test: {
    environment: "jsdom",
    globals: true,
    setupFiles: ["./src/test-setup.ts"],
  },
});
```

**Step 3 — Write `frontend/tsconfig.json`**:
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

Also write `frontend/tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "composite": true,
    "skipLibCheck": true,
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

**Step 4 — Write `frontend/tailwind.config.ts`** with EXACT token extensions from design spec:

```typescript
import type { Config } from "tailwindcss";

export default {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  darkMode: "class",
  theme: {
    extend: {
      colors: {
        border: "hsl(var(--color-border))",
        background: "hsl(var(--color-background))",
        foreground: "hsl(var(--color-foreground))",
        primary: {
          DEFAULT: "hsl(var(--color-primary))",
          foreground: "hsl(var(--color-primary-foreground))",
        },
        secondary: {
          DEFAULT: "hsl(var(--color-secondary))",
          foreground: "hsl(var(--color-secondary-foreground))",
        },
        destructive: {
          DEFAULT: "hsl(var(--color-destructive))",
          foreground: "hsl(var(--color-destructive-foreground))",
        },
        muted: {
          DEFAULT: "hsl(var(--color-muted))",
          foreground: "hsl(var(--color-muted-foreground))",
        },
        card: {
          DEFAULT: "hsl(var(--color-card))",
          foreground: "hsl(var(--color-card-foreground))",
        },
        status: {
          open: "hsl(var(--color-status-open))",
          resolved: "hsl(var(--color-status-resolved))",
          duplicate: "hsl(var(--color-status-duplicate))",
          bogus: "hsl(var(--color-status-bogus))",
        },
      },
      borderRadius: {
        sm: "calc(var(--radius) - 2px)",
        md: "var(--radius)",
        lg: "calc(var(--radius) + 2px)",
        xl: "calc(var(--radius) + 6px)",
      },
      boxShadow: {
        sm: "0 1px 2px 0 rgb(0 0 0 / 0.05)",
        md: "0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1)",
        lg: "0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1)",
      },
      fontFamily: {
        sans: ["Inter", "system-ui", "sans-serif"],
        mono: ["JetBrains Mono", "monospace"],
      },
    },
  },
  plugins: [],
} satisfies Config;
```

**Step 5 — Write `frontend/postcss.config.js`**:
```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

**Step 6 — Write `frontend/index.html`**:
```html
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>uReport CRM</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

**Step 7 — Write `frontend/src/globals.css`** with EXACT CSS custom property tokens (copy verbatim — do NOT paraphrase):

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@import "@fontsource/inter/400.css";
@import "@fontsource/inter/500.css";
@import "@fontsource/inter/600.css";
@import "@fontsource/inter/700.css";
@import "@fontsource/jetbrains-mono/400.css";
@import "@fontsource/jetbrains-mono/500.css";

@layer base {
  :root {
    --color-primary: 221 83% 53%;
    --color-primary-foreground: 0 0% 100%;
    --color-secondary: 210 40% 96%;
    --color-secondary-foreground: 222 47% 11%;
    --color-destructive: 0 84% 60%;
    --color-destructive-foreground: 0 0% 100%;
    --color-muted: 210 40% 96%;
    --color-muted-foreground: 215 16% 47%;
    --color-accent: 210 40% 96%;
    --color-background: 0 0% 100%;
    --color-foreground: 222 47% 11%;
    --color-border: 214 32% 91%;
    --color-ring: 221 83% 53%;
    --color-card: 0 0% 100%;
    --color-card-foreground: 222 47% 11%;
    --color-popover: 0 0% 100%;
    --color-popover-foreground: 222 47% 11%;
    /* Status colors */
    --color-status-open: 217 91% 60%;
    --color-status-resolved: 142 71% 45%;
    --color-status-duplicate: 215 16% 47%;
    --color-status-bogus: 0 84% 60%;
    /* Border radius */
    --radius: 0.375rem;
  }

  .dark {
    --color-background: 222 47% 11%;
    --color-foreground: 210 40% 98%;
    --color-border: 217 32% 18%;
    --color-card: 222 47% 14%;
    --color-card-foreground: 210 40% 98%;
    --color-primary: 217 91% 65%;
    --color-primary-foreground: 222 47% 11%;
    --color-secondary: 217 32% 18%;
    --color-secondary-foreground: 210 40% 98%;
    --color-muted: 217 32% 18%;
    --color-muted-foreground: 215 20% 65%;
    --color-accent: 217 32% 18%;
    --color-accent-foreground: 210 40% 98%;
    --color-popover: 222 47% 14%;
    --color-popover-foreground: 210 40% 98%;
    /* Status colors dark */
    --color-status-open: 217 91% 65%;
    --color-status-resolved: 142 71% 50%;
    --color-status-duplicate: 215 20% 65%;
    --color-status-bogus: 0 84% 65%;
  }

  * {
    @apply border-border;
  }

  body {
    @apply bg-background text-foreground font-sans;
    font-synthesis: none;
    text-rendering: optimizeLegibility;
    -webkit-font-smoothing: antialiased;
  }
}
```

**Step 8 — Write `frontend/src/lib/utils.ts`**:
```typescript
import { clsx, type ClassValue } from "clsx";
import { twMerge } from "tailwind-merge";

export function cn(...inputs: ClassValue[]): string {
  return twMerge(clsx(inputs));
}
```

**Step 9 — Write `frontend/src/lib/animations.ts`** with EXACT Framer Motion presets (copy verbatim — durations MUST be ≤ 0.3s):

```typescript
import type { Variants } from "framer-motion";

export const pageVariants: Variants = {
  initial: { opacity: 0, y: 8 },
  animate: { opacity: 1, y: 0, transition: { duration: 0.2 } },
  exit: { opacity: 0, y: -8, transition: { duration: 0.15 } },
};

export const containerVariants: Variants = {
  animate: { transition: { staggerChildren: 0.05 } },
};

export const itemVariants: Variants = {
  initial: { opacity: 0, y: 4 },
  animate: { opacity: 1, y: 0, transition: { duration: 0.15 } },
};

export const stepVariants: Variants = {
  enter: (dir: number) => ({ x: dir > 0 ? 40 : -40, opacity: 0 }),
  center: { x: 0, opacity: 1, transition: { duration: 0.25 } },
  exit: (dir: number) => ({ x: dir > 0 ? -40 : 40, opacity: 0, transition: { duration: 0.2 } }),
};
```

**Step 10 — Write `frontend/src/lib/api.ts`** (Axios base client):
```typescript
import axios from "axios";

const api = axios.create({
  baseURL: "/api",
  withCredentials: true, // send httpOnly JWT cookie
  headers: { "Content-Type": "application/json" },
});

export default api;
```

**Step 11 — Write `frontend/src/main.tsx`**:
```typescript
import React from "react";
import ReactDOM from "react-dom/client";
import { BrowserRouter } from "react-router-dom";
import "./globals.css";
import App from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <BrowserRouter>
      <App />
    </BrowserRouter>
  </React.StrictMode>
);
```

**Step 12 — Write `frontend/src/App.tsx`** (minimal routing + AnimatePresence shell):
```typescript
import { Routes, Route, useLocation } from "react-router-dom";
import { AnimatePresence } from "framer-motion";
import { ThemeProvider } from "./contexts/ThemeContext";
import { AuthProvider } from "./contexts/AuthContext";
import AppShell from "./components/shell/AppShell";
import LoginPage from "./pages/LoginPage";
import DashboardPage from "./pages/DashboardPage";

export default function App() {
  const location = useLocation();

  return (
    <ThemeProvider>
      <AuthProvider>
        <AnimatePresence mode="wait">
          <Routes location={location} key={location.pathname}>
            <Route path="/login" element={<LoginPage />} />
            <Route element={<AppShell />}>
              <Route path="/dashboard" element={<DashboardPage />} />
              <Route path="/" element={<DashboardPage />} />
            </Route>
          </Routes>
        </AnimatePresence>
      </AuthProvider>
    </ThemeProvider>
  );
}
```

**Step 13 — Write `frontend/src/components/shell/AppShell.tsx`** (placeholder for Task 1 — full implementation in 07-03):
```typescript
import { Outlet } from "react-router-dom";

export default function AppShell() {
  return (
    <div className="flex h-screen bg-background">
      <main id="main-content" className="flex-1 overflow-auto p-4">
        <Outlet />
      </main>
    </div>
  );
}
```

**Step 14 — Write `frontend/src/pages/LoginPage.tsx`** (placeholder — full auth UI in Phase 9):
```typescript
export default function LoginPage() {
  return (
    <div className="flex h-screen items-center justify-center bg-background">
      <div className="text-center">
        <h1 className="text-2xl font-semibold text-foreground">uReport CRM</h1>
        <p className="mt-2 text-muted-foreground">Authentication coming in Phase 9</p>
      </div>
    </div>
  );
}
```

**Step 15 — Write `frontend/src/pages/DashboardPage.tsx`** (placeholder — full dashboard in Phase 8):
```typescript
import { motion } from "framer-motion";
import { pageVariants } from "../lib/animations";

export default function DashboardPage() {
  return (
    <motion.div variants={pageVariants} initial="initial" animate="animate" exit="exit">
      <h1 className="text-2xl font-semibold text-foreground">Dashboard</h1>
      <p className="mt-2 text-muted-foreground">Dashboard coming in Phase 8</p>
    </motion.div>
  );
}
```

**Step 16 — Write `frontend/src/test-setup.ts`**:
```typescript
import "@testing-library/jest-dom";
```
  </action>
  <verify>
```bash
cd frontend && npm install && npm run build 2>&1 | tail -20 && echo "BUILD OK"
grep -n 'color-primary' frontend/src/globals.css | head -5 && echo "TOKENS OK"
grep -n '\.dark' frontend/src/globals.css | head -3 && echo "DARK OK"
grep -n 'hsl(var(--color-primary))' frontend/tailwind.config.ts && echo "TAILWIND OK"
grep -n 'host.*0\.0\.0\.0' frontend/vite.config.ts && echo "VITE HOST OK"
grep -n 'export const pageVariants' frontend/src/lib/animations.ts && echo "ANIMATIONS OK"
```
  </verify>
  <done>
- `npm install` succeeds (no peer dep errors)
- `npm run build` exits 0 with no TypeScript errors
- `frontend/src/globals.css` contains all CSS variable definitions for both `:root` (light) and `.dark` (dark mode) — verified by grep
- `frontend/tailwind.config.ts` contains `hsl(var(--color-primary))` and all CSS variable color references
- `frontend/vite.config.ts` contains `host: "0.0.0.0"` and `port: 5173`
- `frontend/src/lib/animations.ts` exports all 4 named variants with durations ≤ 0.3s
  </done>
</task>

<task type="auto">
  <name>Task 2: ThemeContext, AuthContext, and unit tests</name>
  <files>
    frontend/src/contexts/ThemeContext.tsx
    frontend/src/contexts/AuthContext.tsx
    frontend/src/__tests__/theme.test.tsx
    frontend/src/__tests__/tokens.test.ts
  </files>
  <action>
**Step 1 — Write `frontend/src/contexts/ThemeContext.tsx`**:

Theme stored in localStorage under key `'ureport-theme'`; values: `'light' | 'dark' | 'system'`. When system, read `prefers-color-scheme`. Apply/remove `.dark` class on `document.documentElement` (not `document.body`).

```typescript
import { createContext, useContext, useEffect, useState } from "react";

export type Theme = "light" | "dark" | "system";

interface ThemeContextValue {
  theme: Theme;
  setTheme: (theme: Theme) => void;
  resolvedTheme: "light" | "dark";
}

const ThemeContext = createContext<ThemeContextValue | undefined>(undefined);

const STORAGE_KEY = "ureport-theme";

function getSystemTheme(): "light" | "dark" {
  if (typeof window === "undefined") return "light";
  return window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";
}

function resolveTheme(theme: Theme): "light" | "dark" {
  return theme === "system" ? getSystemTheme() : theme;
}

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const [theme, setThemeState] = useState<Theme>(() => {
    const stored = localStorage.getItem(STORAGE_KEY) as Theme | null;
    return stored ?? "system";
  });

  const resolvedTheme = resolveTheme(theme);

  useEffect(() => {
    const root = document.documentElement;
    if (resolvedTheme === "dark") {
      root.classList.add("dark");
    } else {
      root.classList.remove("dark");
    }
  }, [resolvedTheme]);

  // Re-resolve when system preference changes
  useEffect(() => {
    if (theme !== "system") return;
    const mql = window.matchMedia("(prefers-color-scheme: dark)");
    const handler = () => {
      const root = document.documentElement;
      if (mql.matches) root.classList.add("dark");
      else root.classList.remove("dark");
    };
    mql.addEventListener("change", handler);
    return () => mql.removeEventListener("change", handler);
  }, [theme]);

  function setTheme(newTheme: Theme) {
    localStorage.setItem(STORAGE_KEY, newTheme);
    setThemeState(newTheme);
  }

  return (
    <ThemeContext.Provider value={{ theme, setTheme, resolvedTheme }}>
      {children}
    </ThemeContext.Provider>
  );
}

export function useTheme(): ThemeContextValue {
  const ctx = useContext(ThemeContext);
  if (!ctx) throw new Error("useTheme must be used inside ThemeProvider");
  return ctx;
}
```

**Step 2 — Write `frontend/src/contexts/AuthContext.tsx`**:

Calls `GET /api/auth/me` on mount. On 401 redirects to `/login`. Stores user with exact field names: `personId`, `username`, `role`, `firstname`, `lastname`.

```typescript
import { createContext, useContext, useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import api from "../lib/api";

export interface User {
  personId: number;
  username: string;
  role: "staff" | "admin";
  firstname: string;
  lastname: string;
}

interface AuthContextValue {
  user: User | null;
  loading: boolean;
  logout: () => Promise<void>;
}

const AuthContext = createContext<AuthContextValue | undefined>(undefined);

export function AuthProvider({ children }: { children: React.ReactNode }) {
  const [user, setUser] = useState<User | null>(null);
  const [loading, setLoading] = useState(true);
  const navigate = useNavigate();

  useEffect(() => {
    api
      .get<User>("/auth/me")
      .then((res) => setUser(res.data))
      .catch((err) => {
        if (err.response?.status === 401) {
          navigate("/login", { replace: true });
        }
      })
      .finally(() => setLoading(false));
  }, [navigate]);

  async function logout() {
    await api.post("/auth/logout").catch(() => {});
    setUser(null);
    navigate("/login", { replace: true });
  }

  return (
    <AuthContext.Provider value={{ user, loading, logout }}>
      {children}
    </AuthContext.Provider>
  );
}

export function useAuth(): AuthContextValue {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error("useAuth must be used inside AuthProvider");
  return ctx;
}
```

**Step 3 — Write `frontend/src/__tests__/theme.test.tsx`**:

```typescript
import { render, screen, act } from "@testing-library/react";
import { ThemeProvider, useTheme } from "../contexts/ThemeContext";

// Consumer component for testing
function ThemeConsumer() {
  const { theme, setTheme, resolvedTheme } = useTheme();
  return (
    <div>
      <span data-testid="theme">{theme}</span>
      <span data-testid="resolved">{resolvedTheme}</span>
      <button onClick={() => setTheme("dark")}>Set Dark</button>
      <button onClick={() => setTheme("light")}>Set Light</button>
    </div>
  );
}

function Wrapper({ children }: { children: React.ReactNode }) {
  return <ThemeProvider>{children}</ThemeProvider>;
}

beforeEach(() => {
  localStorage.clear();
  document.documentElement.classList.remove("dark");
});

test("defaults to system theme when localStorage is empty", () => {
  render(<ThemeConsumer />, { wrapper: Wrapper });
  expect(screen.getByTestId("theme").textContent).toBe("system");
});

test("setting theme to dark adds .dark class to <html>", () => {
  render(<ThemeConsumer />, { wrapper: Wrapper });
  act(() => {
    screen.getByText("Set Dark").click();
  });
  expect(document.documentElement.classList.contains("dark")).toBe(true);
  expect(localStorage.getItem("ureport-theme")).toBe("dark");
});

test("setting theme to light removes .dark class from <html>", () => {
  document.documentElement.classList.add("dark");
  render(<ThemeConsumer />, { wrapper: Wrapper });
  act(() => {
    screen.getByText("Set Light").click();
  });
  expect(document.documentElement.classList.contains("dark")).toBe(false);
  expect(localStorage.getItem("ureport-theme")).toBe("light");
});

test("persists theme choice to localStorage under key 'ureport-theme'", () => {
  render(<ThemeConsumer />, { wrapper: Wrapper });
  act(() => {
    screen.getByText("Set Dark").click();
  });
  expect(localStorage.getItem("ureport-theme")).toBe("dark");
});

test("restores theme from localStorage on mount", () => {
  localStorage.setItem("ureport-theme", "dark");
  render(<ThemeConsumer />, { wrapper: Wrapper });
  expect(screen.getByTestId("theme").textContent).toBe("dark");
  expect(document.documentElement.classList.contains("dark")).toBe(true);
});

test("throws when useTheme used outside ThemeProvider", () => {
  // Suppress console.error for this test
  const spy = vi.spyOn(console, "error").mockImplementation(() => {});
  expect(() => render(<ThemeConsumer />)).toThrow("useTheme must be used inside ThemeProvider");
  spy.mockRestore();
});
```

**Step 4 — Write `frontend/src/__tests__/tokens.test.ts`**:

```typescript
import { readFileSync } from "fs";
import { resolve } from "path";

const css = readFileSync(resolve(__dirname, "../globals.css"), "utf-8");

describe("CSS design tokens", () => {
  test("globals.css contains all light-mode custom properties in :root", () => {
    const required = [
      "--color-primary",
      "--color-primary-foreground",
      "--color-secondary",
      "--color-secondary-foreground",
      "--color-destructive",
      "--color-muted",
      "--color-muted-foreground",
      "--color-background",
      "--color-foreground",
      "--color-border",
      "--color-ring",
      "--color-card",
      "--color-card-foreground",
      "--color-status-open",
      "--color-status-resolved",
      "--color-status-duplicate",
      "--color-status-bogus",
      "--radius",
    ];
    for (const token of required) {
      expect(css).toContain(token);
    }
  });

  test("globals.css has .dark section with dark-mode overrides", () => {
    expect(css).toContain(".dark {");
    expect(css).toContain("--color-background: 222 47% 11%");
    expect(css).toContain("--color-primary: 217 91% 65%");
    expect(css).toContain("--color-status-open: 217 91% 65%");
  });

  test("all animation durations are within 300ms budget", () => {
    const { readFileSync: rfs } = require("fs");
    const animTs = rfs(resolve(__dirname, "../lib/animations.ts"), "utf-8");
    const durations = [...animTs.matchAll(/duration:\s*([\d.]+)/g)].map(
      (m) => parseFloat(m[1])
    );
    expect(durations.length).toBeGreaterThan(0);
    for (const d of durations) {
      expect(d).toBeLessThanOrEqual(0.3);
    }
  });
});
```
  </action>
  <verify>
```bash
cd frontend && npm run test -- --reporter=verbose 2>&1 | tail -30 && echo "TESTS OK"
grep -c 'color-' frontend/src/globals.css && echo "TOKEN COUNT OK"
```
  </verify>
  <done>
- All theme tests pass: defaults to system, sets dark class on html, removes dark class, persists to localStorage, restores from localStorage, throws outside provider
- Token test confirms all 18 CSS custom properties exist in globals.css
- Token test confirms `.dark` section exists with correct overrides
- Animation duration test confirms all durations ≤ 0.3s
- `npm run test` exits 0 (all tests pass, none skipped)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→API | AuthContext calls GET /api/auth/me and POST /api/auth/logout — response data flows into React state |
| localStorage→app | Theme preference stored in localStorage is read on mount and applied to the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Information disclosure | AuthContext GET /api/auth/me response → React state | transfer | Server owns the JWT validation and only returns user fields after verifying the httpOnly cookie (Phase 2 backend); frontend never inspects the JWT itself — no client-side token parsing |
| T-07-02 | Tampering | localStorage 'ureport-theme' → classList manipulation | accept | Only 3 valid values ('light'/'dark'/'system') are acted upon; any other stored string resolves to system theme via `resolveTheme()`; no code execution path from localStorage value — risk is cosmetic only, owned by frontend |
| T-07-03 | Spoofing | AuthContext user.role → role-based nav display | mitigate | Role value comes from trusted `GET /api/auth/me` response (server-set, not client-mutable); in `AuthContext.tsx::AuthProvider` the response data is typed as `User` with `role: "staff" | "admin"` and used only for UI display, never for server-side access control |
| T-07-04 | Information disclosure | axios base client sends credentials to /api | mitigate | `api.ts` sets `withCredentials: true` so the httpOnly JWT cookie is included; axios base URL is `/api` (relative, not an external host), preventing cross-origin credential leakage; Vite proxy in `vite.config.ts` proxies /api to localhost:8080 only in dev — in production nginx handles the proxy |
</threat_model>

<verification>
1. `cd frontend && npm install` exits 0 with no peer dependency errors
2. `npm run build` exits 0 with no TypeScript compilation errors
3. `npm run test` exits 0 with all tests passing (theme tests + token tests)
4. `grep -c '--color-' frontend/src/globals.css` returns ≥ 18 (all design tokens defined)
5. `grep '\.dark' frontend/src/globals.css` shows the dark mode block
6. `grep 'hsl(var(--color-primary))' frontend/tailwind.config.ts` finds the CSS variable reference
7. `grep 'host.*0\.0\.0\.0' frontend/vite.config.ts` confirms sandbox bind address
8. `grep 'export const pageVariants' frontend/src/lib/animations.ts` finds all 4 variant exports
</verification>

<success_criteria>
- React + Vite + TypeScript project scaffolded in `frontend/` and builds successfully
- `src/globals.css` has all 18 CSS custom properties in `:root` and full dark mode overrides in `.dark`
- `tailwind.config.ts` extends all colors using `hsl(var(--color-*))` references
- ThemeProvider correctly toggles `.dark` on `<html>` and persists to localStorage `'ureport-theme'`
- AuthContext calls `GET /api/auth/me` on mount and redirects to `/login` on 401
- All 4 Framer Motion variant presets exported from `lib/animations.ts` with durations ≤ 0.3s
- 6 unit tests pass (5 theme + 1 token suite)
- Vite dev server configured for 0.0.0.0:5173 (no X-Frame-Options — sandbox iframe compatible)
- Fonts loaded from @fontsource (no Google CDN — offline compatible)
</success_criteria>

<output>
After completion, create `.planning/phases/07-react-design-system-and-shell/07-01-SUMMARY.md` following the summary template.
</output>
