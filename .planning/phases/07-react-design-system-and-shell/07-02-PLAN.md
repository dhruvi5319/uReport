---
phase: 07-react-design-system-and-shell
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/ui/button.tsx
  - frontend/src/components/ui/badge.tsx
  - frontend/src/components/ui/card.tsx
  - frontend/src/components/ui/dialog.tsx
  - frontend/src/components/ui/input.tsx
  - frontend/src/components/ui/select.tsx
  - frontend/src/components/ui/table.tsx
  - frontend/src/components/ui/skeleton.tsx
  - frontend/src/components/ui/toast.tsx
  - frontend/src/components/ui/toaster.tsx
  - frontend/src/components/ui/sheet.tsx
  - frontend/src/components/ui/tabs.tsx
  - frontend/src/components/ui/popover.tsx
  - frontend/src/components/ui/command.tsx
  - frontend/src/components/ui/alert-dialog.tsx
  - frontend/src/components/ui/avatar.tsx
  - frontend/src/components/ui/breadcrumb.tsx
  - frontend/src/components/ui/index.ts
  - frontend/src/components/ui/__tests__/components.test.tsx
autonomous: true

features:
  implements: ["F17", "F19"]
  depends_on: []
  enables: ["F18"]

must_haves:
  truths:
    - "All 12+ shadcn/ui components are vendored into frontend/src/components/ui/"
    - "Badge has a 'status' variant with open (blue), resolved (green), duplicate (gray), bogus (red) using CSS token colors"
    - "Button has a 2px solid primary focus ring with 2px offset"
    - "Input has correct dark mode background using CSS tokens"
    - "All components pass axe-core accessibility scan with 0 critical violations"
    - "components/ui/index.ts re-exports all vendored components"
  artifacts:
    - path: "frontend/src/components/ui/badge.tsx"
      provides: "Badge component with status variant"
      contains: "status"
    - path: "frontend/src/components/ui/button.tsx"
      provides: "Button component with focus ring"
      contains: "focus-visible:ring-2"
    - path: "frontend/src/components/ui/index.ts"
      provides: "Re-exports for all shadcn/ui components"
      exports: ["Button", "Badge", "Card", "Dialog", "Input", "Select", "Table", "Skeleton", "Sheet", "Tabs", "Popover", "Command", "AlertDialog", "Avatar", "Breadcrumb"]
    - path: "frontend/src/components/ui/__tests__/components.test.tsx"
      provides: "Vitest + RTL + jest-axe accessibility tests"
      contains: "axe"
  key_links:
    - from: "frontend/src/components/ui/badge.tsx"
      to: "CSS --color-status-* tokens"
      via: "cva variant classes using Tailwind status-* colors"
      pattern: "status-open|status-resolved|status-duplicate|status-bogus"
    - from: "frontend/src/components/ui/__tests__/components.test.tsx"
      to: "jest-axe"
      via: "toHaveNoViolations() assertion"
      pattern: "toHaveNoViolations"

integration_contracts:
  requires: []
  provides:
    - artifact: "frontend/src/components/ui/index.ts"
      exports: ["Button", "Badge", "Card", "CardHeader", "CardContent", "CardFooter", "CardTitle", "CardDescription", "Dialog", "DialogTrigger", "DialogContent", "DialogHeader", "DialogTitle", "DialogDescription", "DialogFooter", "Input", "Select", "SelectTrigger", "SelectValue", "SelectContent", "SelectItem", "Table", "TableHeader", "TableBody", "TableRow", "TableHead", "TableCell", "Skeleton", "Sheet", "SheetTrigger", "SheetContent", "Tabs", "TabsList", "TabsTrigger", "TabsContent", "Popover", "PopoverTrigger", "PopoverContent", "Command", "CommandInput", "CommandList", "CommandItem", "AlertDialog", "AlertDialogTrigger", "AlertDialogContent", "AlertDialogHeader", "AlertDialogTitle", "AlertDialogDescription", "AlertDialogFooter", "AlertDialogCancel", "AlertDialogAction", "Avatar", "AvatarImage", "AvatarFallback", "Breadcrumb", "BreadcrumbList", "BreadcrumbItem", "BreadcrumbLink", "BreadcrumbPage", "BreadcrumbSeparator", "Toast", "Toaster"]
      shape: "Re-export barrel file for all vendored shadcn/ui components"
      verify: "grep -n 'export.*Button' frontend/src/components/ui/index.ts && grep -n 'export.*Badge' frontend/src/components/ui/index.ts && grep -n 'export.*Sheet' frontend/src/components/ui/index.ts && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/badge.tsx"
      exports: ["Badge", "badgeVariants"]
      shape: |
        Badge accepts variant: 'default' | 'secondary' | 'destructive' | 'outline' | 'status'
        and statusVariant: 'open' | 'resolved' | 'duplicate' | 'bogus' when variant='status'
      verify: "grep -n 'status' frontend/src/components/ui/badge.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/sheet.tsx"
      exports: ["Sheet", "SheetTrigger", "SheetContent", "SheetHeader", "SheetTitle", "SheetDescription", "SheetFooter", "SheetClose"]
      shape: "Radix Dialog-based Sheet with side variants (top/right/bottom/left)"
      verify: "grep -n 'SheetContent' frontend/src/components/ui/sheet.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/avatar.tsx"
      exports: ["Avatar", "AvatarImage", "AvatarFallback"]
      shape: "Radix Avatar with image and fallback"
      verify: "grep -n 'AvatarFallback' frontend/src/components/ui/avatar.tsx && echo CONTRACT_OK"
    - artifact: "frontend/src/components/ui/popover.tsx"
      exports: ["Popover", "PopoverTrigger", "PopoverContent"]
      shape: "Radix Popover"
      verify: "grep -n 'PopoverContent' frontend/src/components/ui/popover.tsx && echo CONTRACT_OK"
---

<objective>
Install and vendor all 12+ required shadcn/ui components into `frontend/src/components/ui/`, then customize Badge (status variants), Button (focus ring), and Input (dark mode) to match the civic brand design system. Write axe-core accessibility tests for all components.

Purpose: All subsequent UI plans import from `@/components/ui/` — these components must exist, be correctly styled, and pass accessibility checks before any screens are built.
Output: 16 vendored + customized component files, a barrel re-export index, and a test suite with 0 axe critical violations.
</objective>

<feature_dependencies>
Implements: F17: shadcn/ui components fully customized to city brand (Badge status variants, Button focus ring, Input dark mode). F19: WCAG 2.1 AA — axe-core finds 0 critical violations on all components.
Depends on: None (Wave 1 — parallel with 07-01; CSS tokens consumed via class names, not file imports)
Enables: F18: Navigation shell (needs Sheet, Avatar, Popover, Breadcrumb components)
</feature_dependencies>

<execution_context>
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/workflows/execute-plan.md
@/app/workspaces/.pivota-home/opencode-xdg/opencode/pivota_spec-framework/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Vendor and customize all shadcn/ui components</name>
  <files>
    frontend/src/components/ui/button.tsx
    frontend/src/components/ui/badge.tsx
    frontend/src/components/ui/card.tsx
    frontend/src/components/ui/dialog.tsx
    frontend/src/components/ui/input.tsx
    frontend/src/components/ui/select.tsx
    frontend/src/components/ui/table.tsx
    frontend/src/components/ui/skeleton.tsx
    frontend/src/components/ui/toast.tsx
    frontend/src/components/ui/toaster.tsx
    frontend/src/components/ui/use-toast.ts
    frontend/src/components/ui/sheet.tsx
    frontend/src/components/ui/tabs.tsx
    frontend/src/components/ui/popover.tsx
    frontend/src/components/ui/command.tsx
    frontend/src/components/ui/alert-dialog.tsx
    frontend/src/components/ui/avatar.tsx
    frontend/src/components/ui/breadcrumb.tsx
    frontend/src/components/ui/index.ts
    frontend/components.json
  </files>
  <action>
Vendor all shadcn/ui components by running the shadcn CLI in the `frontend/` directory. The project does NOT use Next.js — use the Vite + React path.

**Step 1 — Initialize shadcn with components.json** (write manually to avoid interactive prompts):

Write `frontend/components.json`:
```json
{
  "$schema": "https://ui.shadcn.com/schema.json",
  "style": "default",
  "rsc": false,
  "tsx": true,
  "tailwind": {
    "config": "tailwind.config.ts",
    "css": "src/globals.css",
    "baseColor": "slate",
    "cssVariables": true,
    "prefix": ""
  },
  "aliases": {
    "components": "@/components",
    "utils": "@/lib/utils"
  }
}
```

**Step 2 — Run shadcn CLI to vendor all required components**:
```bash
cd frontend
npx shadcn@latest add button badge card dialog input select table skeleton toast sheet tabs popover command alert-dialog avatar breadcrumb --yes --overwrite
```

NOTE: The `npx shadcn@latest add` command will install Radix UI packages, generate component files in `src/components/ui/`, and update `package.json`. After this runs, immediately apply the customizations in Steps 3-7.

**Step 3 — Customize `frontend/src/components/ui/button.tsx`**:

After shadcn generates the file, find the `focusVisible` ring classes and ensure they use the correct 2px ring with 2px offset. The key class string in buttonVariants should contain:
```
focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2
```
If shadcn generated different ring classes (e.g., `focus-visible:ring-1`), replace with `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`.

The `ring` color in Tailwind maps to `hsl(var(--color-ring))` from tailwind.config.ts (which you set to `221 83% 53%` — civic blue). Verify this mapping is present in tailwind.config.ts and add if missing:
```typescript
ring: "hsl(var(--color-ring))",
```

**Step 4 — Customize `frontend/src/components/ui/badge.tsx`**:

Replace the shadcn-generated badge with this version that adds a `status` variant using the CSS token colors:

```typescript
import * as React from "react";
import { cva, type VariantProps } from "class-variance-authority";
import { cn } from "@/lib/utils";

const badgeVariants = cva(
  "inline-flex items-center rounded-md border px-2.5 py-0.5 text-xs font-semibold transition-colors focus:outline-none focus:ring-2 focus:ring-ring focus:ring-offset-2",
  {
    variants: {
      variant: {
        default:
          "border-transparent bg-primary text-primary-foreground shadow hover:bg-primary/80",
        secondary:
          "border-transparent bg-secondary text-secondary-foreground hover:bg-secondary/80",
        destructive:
          "border-transparent bg-destructive text-destructive-foreground shadow hover:bg-destructive/80",
        outline: "text-foreground",
        // Status variants use CSS custom property tokens defined in globals.css
        open:
          "border-transparent bg-status-open text-white shadow",
        resolved:
          "border-transparent bg-status-resolved text-white shadow",
        duplicate:
          "border-transparent bg-status-duplicate text-white shadow",
        bogus:
          "border-transparent bg-status-bogus text-white shadow",
      },
    },
    defaultVariants: {
      variant: "default",
    },
  }
);

export interface BadgeProps
  extends React.HTMLAttributes<HTMLDivElement>,
    VariantProps<typeof badgeVariants> {}

function Badge({ className, variant, ...props }: BadgeProps) {
  return (
    <div className={cn(badgeVariants({ variant }), className)} {...props} />
  );
}

export { Badge, badgeVariants };
```

**Step 5 — Customize `frontend/src/components/ui/input.tsx`**:

Ensure the Input component uses dark mode-aware CSS token classes. Replace with:

```typescript
import * as React from "react";
import { cn } from "@/lib/utils";

export interface InputProps
  extends React.InputHTMLAttributes<HTMLInputElement> {}

const Input = React.forwardRef<HTMLInputElement, InputProps>(
  ({ className, type, ...props }, ref) => {
    return (
      <input
        type={type}
        className={cn(
          "flex h-9 w-full rounded-md border border-border bg-background px-3 py-1 text-sm shadow-sm transition-colors",
          "file:border-0 file:bg-transparent file:text-sm file:font-medium",
          "placeholder:text-muted-foreground",
          "focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2",
          "disabled:cursor-not-allowed disabled:opacity-50",
          // Dark mode: background uses --color-background which switches via .dark class
          "dark:border-border dark:bg-background dark:text-foreground",
          className
        )}
        ref={ref}
        {...props}
      />
    );
  }
);
Input.displayName = "Input";

export { Input };
```

**Step 6 — Write `frontend/src/components/ui/index.ts`** barrel re-export (write after all components are vendored):

```typescript
// Button
export { Button, buttonVariants } from "./button";
// Badge
export { Badge, badgeVariants } from "./badge";
// Card
export { Card, CardHeader, CardFooter, CardTitle, CardDescription, CardContent } from "./card";
// Dialog
export {
  Dialog,
  DialogPortal,
  DialogOverlay,
  DialogClose,
  DialogTrigger,
  DialogContent,
  DialogHeader,
  DialogFooter,
  DialogTitle,
  DialogDescription,
} from "./dialog";
// Input
export { Input } from "./input";
// Select
export {
  Select,
  SelectGroup,
  SelectValue,
  SelectTrigger,
  SelectContent,
  SelectLabel,
  SelectItem,
  SelectSeparator,
  SelectScrollUpButton,
  SelectScrollDownButton,
} from "./select";
// Table
export {
  Table,
  TableHeader,
  TableBody,
  TableFooter,
  TableHead,
  TableRow,
  TableCell,
  TableCaption,
} from "./table";
// Skeleton
export { Skeleton } from "./skeleton";
// Toast
export { Toast, ToastAction, ToastClose, ToastDescription, ToastProvider, ToastTitle, ToastViewport } from "./toast";
export { Toaster } from "./toaster";
export { useToast } from "./use-toast";
// Sheet
export {
  Sheet,
  SheetPortal,
  SheetOverlay,
  SheetTrigger,
  SheetClose,
  SheetContent,
  SheetHeader,
  SheetFooter,
  SheetTitle,
  SheetDescription,
} from "./sheet";
// Tabs
export { Tabs, TabsList, TabsTrigger, TabsContent } from "./tabs";
// Popover
export { Popover, PopoverTrigger, PopoverContent } from "./popover";
// Command
export {
  Command,
  CommandDialog,
  CommandInput,
  CommandList,
  CommandEmpty,
  CommandGroup,
  CommandItem,
  CommandShortcut,
  CommandSeparator,
} from "./command";
// AlertDialog
export {
  AlertDialog,
  AlertDialogPortal,
  AlertDialogOverlay,
  AlertDialogTrigger,
  AlertDialogContent,
  AlertDialogHeader,
  AlertDialogFooter,
  AlertDialogTitle,
  AlertDialogDescription,
  AlertDialogAction,
  AlertDialogCancel,
} from "./alert-dialog";
// Avatar
export { Avatar, AvatarImage, AvatarFallback } from "./avatar";
// Breadcrumb
export {
  Breadcrumb,
  BreadcrumbList,
  BreadcrumbItem,
  BreadcrumbLink,
  BreadcrumbPage,
  BreadcrumbSeparator,
  BreadcrumbEllipsis,
} from "./breadcrumb";
```

Also add `ring` to the tailwind.config.ts colors extend if not already present:
```typescript
ring: "hsl(var(--color-ring))",
```

**Step 7 — Add vite path alias** to `vite.config.ts` so `@/components/ui/` and `@/lib/utils` resolve correctly:

```typescript
import { resolve } from "path";
// In defineConfig:
resolve: {
  alias: {
    "@": resolve(__dirname, "./src"),
  },
},
```

Also add to `tsconfig.json`:
```json
"paths": {
  "@/*": ["./src/*"]
}
```
  </action>
  <verify>
```bash
cd frontend && npm install && npm run build 2>&1 | tail -20 && echo "BUILD OK"
ls frontend/src/components/ui/*.tsx | wc -l && echo "files (expect >= 16)"
grep -n 'status-open\|status-resolved\|status-duplicate\|status-bogus' frontend/src/components/ui/badge.tsx && echo "BADGE STATUS VARIANTS OK"
grep -n 'ring-2.*ring-ring' frontend/src/components/ui/button.tsx && echo "BUTTON FOCUS RING OK"
grep -n 'export.*Badge' frontend/src/components/ui/index.ts && grep -n 'export.*Sheet' frontend/src/components/ui/index.ts && echo "INDEX OK"
```
  </verify>
  <done>
- `npm install && npm run build` exits 0 after all components are vendored
- At least 16 `.tsx` files exist in `frontend/src/components/ui/`
- `badge.tsx` contains `status-open`, `status-resolved`, `status-duplicate`, `status-bogus` variant classes using Tailwind `bg-status-*` tokens
- `button.tsx` contains `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`
- `index.ts` re-exports all 16+ components including Button, Badge, Card, Dialog, Input, Select, Table, Skeleton, Sheet, Tabs, Popover, Command, AlertDialog, Avatar, Breadcrumb
- `@/` path alias resolves correctly (build succeeds without import errors)
  </done>
</task>

<task type="auto">
  <name>Task 2: Accessibility and component tests (Vitest + jest-axe)</name>
  <files>
    frontend/src/components/ui/__tests__/components.test.tsx
  </files>
  <action>
Write the component test suite using Vitest + React Testing Library + jest-axe. Tests must cover: correct variant class rendering, dark mode rendering, dialog focus trap, and 0 axe critical violations for all 12 core components.

Write `frontend/src/components/ui/__tests__/components.test.tsx`:

```typescript
import { render, screen, fireEvent, waitFor } from "@testing-library/react";
import { axe, toHaveNoViolations } from "jest-axe";
import { expect, describe, test, vi, beforeAll } from "vitest";
import {
  Button,
  Badge,
  Card,
  CardHeader,
  CardContent,
  CardTitle,
  Dialog,
  DialogTrigger,
  DialogContent,
  DialogTitle,
  DialogDescription,
  Input,
  Select,
  SelectTrigger,
  SelectValue,
  SelectContent,
  SelectItem,
  Table,
  TableHeader,
  TableBody,
  TableRow,
  TableHead,
  TableCell,
  Skeleton,
  Sheet,
  SheetTrigger,
  SheetContent,
  SheetTitle,
  Tabs,
  TabsList,
  TabsTrigger,
  TabsContent,
  Avatar,
  AvatarFallback,
} from "../index";

// Extend Vitest matchers
expect.extend(toHaveNoViolations);

// Mock ResizeObserver (not available in jsdom)
beforeAll(() => {
  global.ResizeObserver = class {
    observe() {}
    unobserve() {}
    disconnect() {}
  };
  // Mock matchMedia
  Object.defineProperty(window, "matchMedia", {
    writable: true,
    value: vi.fn().mockImplementation((query: string) => ({
      matches: false,
      media: query,
      onchange: null,
      addListener: vi.fn(),
      removeListener: vi.fn(),
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
      dispatchEvent: vi.fn(),
    })),
  });
});

// ── Button ───────────────────────────────────────────────────────────────────

describe("Button", () => {
  test("renders with default variant classes", () => {
    const { container } = render(<Button>Click me</Button>);
    const btn = container.querySelector("button");
    expect(btn).toBeInTheDocument();
    expect(btn?.className).toMatch(/bg-primary/);
  });

  test("renders destructive variant", () => {
    const { container } = render(<Button variant="destructive">Delete</Button>);
    expect(container.querySelector("button")?.className).toMatch(/bg-destructive/);
  });

  test("renders outline variant", () => {
    const { container } = render(<Button variant="outline">Cancel</Button>);
    expect(container.querySelector("button")?.className).toMatch(/border/);
  });

  test("has focus-visible ring classes", () => {
    const { container } = render(<Button>Focus me</Button>);
    const className = container.querySelector("button")?.className ?? "";
    expect(className).toMatch(/focus-visible:ring-2/);
  });

  test("is disabled when disabled prop set", () => {
    render(<Button disabled>Disabled</Button>);
    expect(screen.getByRole("button")).toBeDisabled();
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(<Button>Accessible Button</Button>);
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Badge ────────────────────────────────────────────────────────────────────

describe("Badge", () => {
  test("renders with default variant", () => {
    const { container } = render(<Badge>Default</Badge>);
    expect(container.firstChild).toBeInTheDocument();
  });

  test("renders 'open' status variant with blue background", () => {
    const { container } = render(<Badge variant="open">Open</Badge>);
    expect(container.firstChild?.toString()).toBeTruthy();
    // Class should contain status-open (maps to --color-status-open)
    const className = (container.firstChild as HTMLElement)?.className ?? "";
    expect(className).toMatch(/status-open/);
  });

  test("renders 'resolved' status variant", () => {
    const { container } = render(<Badge variant="resolved">Resolved</Badge>);
    const className = (container.firstChild as HTMLElement)?.className ?? "";
    expect(className).toMatch(/status-resolved/);
  });

  test("renders 'duplicate' status variant", () => {
    const { container } = render(<Badge variant="duplicate">Duplicate</Badge>);
    const className = (container.firstChild as HTMLElement)?.className ?? "";
    expect(className).toMatch(/status-duplicate/);
  });

  test("renders 'bogus' status variant", () => {
    const { container } = render(<Badge variant="bogus">Bogus</Badge>);
    const className = (container.firstChild as HTMLElement)?.className ?? "";
    expect(className).toMatch(/status-bogus/);
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(
      <div>
        <Badge>Default</Badge>
        <Badge variant="open">Open</Badge>
        <Badge variant="resolved">Resolved</Badge>
      </div>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Card ─────────────────────────────────────────────────────────────────────

describe("Card", () => {
  test("renders card with header and content", () => {
    const { container } = render(
      <Card>
        <CardHeader>
          <CardTitle>Test Card</CardTitle>
        </CardHeader>
        <CardContent>Content here</CardContent>
      </Card>
    );
    expect(screen.getByText("Test Card")).toBeInTheDocument();
    expect(screen.getByText("Content here")).toBeInTheDocument();
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(
      <Card>
        <CardHeader><CardTitle>Accessible Card</CardTitle></CardHeader>
        <CardContent>Card content</CardContent>
      </Card>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Input ────────────────────────────────────────────────────────────────────

describe("Input", () => {
  test("renders with correct type", () => {
    render(<Input type="email" placeholder="Email" aria-label="Email" />);
    const input = screen.getByRole("textbox");
    expect(input).toBeInTheDocument();
  });

  test("has focus ring classes", () => {
    const { container } = render(<Input aria-label="Test input" />);
    const input = container.querySelector("input");
    expect(input?.className).toMatch(/focus-visible:ring-2/);
  });

  test("has dark mode background class", () => {
    const { container } = render(<Input aria-label="Dark input" />);
    const input = container.querySelector("input");
    expect(input?.className).toMatch(/bg-background/);
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(
      <div>
        <label htmlFor="test-input">Email</label>
        <Input id="test-input" type="email" />
      </div>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Table ────────────────────────────────────────────────────────────────────

describe("Table", () => {
  test("renders table with header and body", () => {
    render(
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>ID</TableHead>
            <TableHead>Status</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          <TableRow>
            <TableCell>1</TableCell>
            <TableCell>Open</TableCell>
          </TableRow>
        </TableBody>
      </Table>
    );
    expect(screen.getByText("ID")).toBeInTheDocument();
    expect(screen.getByText("Open")).toBeInTheDocument();
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(
      <Table>
        <TableHeader>
          <TableRow>
            <TableHead>Case ID</TableHead>
          </TableRow>
        </TableHeader>
        <TableBody>
          <TableRow>
            <TableCell>CRM-001</TableCell>
          </TableRow>
        </TableBody>
      </Table>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Skeleton ──────────────────────────────────────────────────────────────────

describe("Skeleton", () => {
  test("renders skeleton element", () => {
    const { container } = render(<Skeleton className="h-4 w-24" />);
    expect(container.firstChild).toBeInTheDocument();
    expect((container.firstChild as HTMLElement)?.className).toMatch(/animate-pulse/);
  });
});

// ── Avatar ───────────────────────────────────────────────────────────────────

describe("Avatar", () => {
  test("renders avatar with fallback", () => {
    render(
      <Avatar>
        <AvatarFallback>JD</AvatarFallback>
      </Avatar>
    );
    expect(screen.getByText("JD")).toBeInTheDocument();
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(
      <Avatar aria-label="John Doe avatar">
        <AvatarFallback>JD</AvatarFallback>
      </Avatar>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Tabs ─────────────────────────────────────────────────────────────────────

describe("Tabs", () => {
  test("renders tabs and switches content on click", () => {
    render(
      <Tabs defaultValue="tab1">
        <TabsList>
          <TabsTrigger value="tab1">Tab 1</TabsTrigger>
          <TabsTrigger value="tab2">Tab 2</TabsTrigger>
        </TabsList>
        <TabsContent value="tab1">Content 1</TabsContent>
        <TabsContent value="tab2">Content 2</TabsContent>
      </Tabs>
    );
    expect(screen.getByText("Content 1")).toBeInTheDocument();
    fireEvent.click(screen.getByText("Tab 2"));
    expect(screen.getByText("Content 2")).toBeInTheDocument();
  });

  test("passes axe accessibility scan", async () => {
    const { container } = render(
      <Tabs defaultValue="cases">
        <TabsList>
          <TabsTrigger value="cases">Cases</TabsTrigger>
          <TabsTrigger value="admin">Admin</TabsTrigger>
        </TabsList>
        <TabsContent value="cases">Cases content</TabsContent>
        <TabsContent value="admin">Admin content</TabsContent>
      </Tabs>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── Dialog ───────────────────────────────────────────────────────────────────

describe("Dialog", () => {
  test("opens dialog on trigger click and closes on cancel", async () => {
    render(
      <Dialog>
        <DialogTrigger asChild>
          <Button>Open Dialog</Button>
        </DialogTrigger>
        <DialogContent>
          <DialogTitle>Test Dialog</DialogTitle>
          <DialogDescription>Dialog description for accessibility</DialogDescription>
          <p>Dialog content</p>
        </DialogContent>
      </Dialog>
    );
    const trigger = screen.getByText("Open Dialog");
    fireEvent.click(trigger);
    await waitFor(() => {
      expect(screen.getByText("Test Dialog")).toBeInTheDocument();
    });
  });

  test("passes axe accessibility scan when open", async () => {
    const { container } = render(
      <Dialog defaultOpen>
        <DialogTrigger asChild>
          <Button>Open</Button>
        </DialogTrigger>
        <DialogContent>
          <DialogTitle>Accessible Dialog</DialogTitle>
          <DialogDescription>This dialog is accessible</DialogDescription>
        </DialogContent>
      </Dialog>
    );
    const results = await axe(container);
    expect(results).toHaveNoViolations();
  });
});

// ── All components batch axe scan ─────────────────────────────────────────────

describe("All core components — batch axe scan", () => {
  test("12 core components render with 0 critical axe violations", async () => {
    const { container } = render(
      <div>
        <Button>Submit</Button>
        <Button variant="outline">Cancel</Button>
        <Badge>New</Badge>
        <Badge variant="open">Open</Badge>
        <Card>
          <CardHeader><CardTitle>Case #001</CardTitle></CardHeader>
          <CardContent>Description of the case</CardContent>
        </Card>
        <div>
          <label htmlFor="batch-input">Search</label>
          <Input id="batch-input" placeholder="Search cases..." />
        </div>
        <Table>
          <TableHeader>
            <TableRow><TableHead scope="col">ID</TableHead><TableHead scope="col">Status</TableHead></TableRow>
          </TableHeader>
          <TableBody>
            <TableRow><TableCell>001</TableCell><TableCell>Open</TableCell></TableRow>
          </TableBody>
        </Table>
        <Skeleton className="h-4 w-48" />
        <Tabs defaultValue="a">
          <TabsList><TabsTrigger value="a">Tab A</TabsTrigger></TabsList>
          <TabsContent value="a">Tab A content</TabsContent>
        </Tabs>
        <Avatar aria-label="User JD">
          <AvatarFallback>JD</AvatarFallback>
        </Avatar>
      </div>
    );
    const results = await axe(container, {
      rules: {
        // Only fail on critical and serious violations
        "color-contrast": { enabled: false }, // CSS vars not resolved in jsdom
      },
    });
    // Filter to only critical/serious violations
    const critical = (results.violations ?? []).filter(
      (v) => v.impact === "critical" || v.impact === "serious"
    );
    expect(critical).toHaveLength(0);
  });
});
```
  </action>
  <verify>
```bash
cd frontend && npm run test -- --reporter=verbose 2>&1 | tail -40 && echo "COMPONENT TESTS OK"
grep -c 'toHaveNoViolations' frontend/src/components/ui/__tests__/components.test.tsx && echo "AXE ASSERTIONS PRESENT"
```
  </verify>
  <done>
- All component tests pass (0 failures, 0 skipped)
- Badge status variant tests confirm `status-open`, `status-resolved`, `status-duplicate`, `status-bogus` class names
- Button test confirms `focus-visible:ring-2` in className
- Dialog test confirms opens on trigger click
- Batch axe scan finds 0 critical/serious violations across all 12 core components
- `npm run test` exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| db→render | shadcn/ui Table and Badge components will later render data fetched from the API — dynamic content crossing into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | Badge status variant prop → className injection | mitigate | Badge uses `cva()` variant maps with fixed class strings — the `variant` prop is typed as a TypeScript union literal; arbitrary strings cannot inject new CSS classes; mitigated in `badge.tsx::badgeVariants` via CVA variant-key lookup |
| T-07-06 | Information disclosure | Dialog/AlertDialog focus trap exposes content to AT | accept | Focus trap is the intended WCAG behavior for modal dialogs (ARIA 1.1 §3.25); screen reader access to dialog content is correct accessibility, not a vulnerability; owned by product |
| T-07-07 | Elevation of privilege | Command palette (cmdk) renders arbitrary href links | mitigate | Command items are rendered as `<button>` elements (not `<a>` elements) in the shadcn Command component; href navigation is driven by `router.push()` with typed route strings, not user-supplied URLs; mitigated in `command.tsx` — no `dangerouslySetInnerHTML` and no dynamic href construction from user input |
</threat_model>

<verification>
1. `npm install && npm run build` exits 0 after all 16+ components are vendored
2. `ls frontend/src/components/ui/*.tsx | wc -l` returns ≥ 16
3. `badge.tsx` contains `status-open`, `status-resolved`, `status-duplicate`, `status-bogus`
4. `button.tsx` contains `focus-visible:ring-2 focus-visible:ring-ring focus-visible:ring-offset-2`
5. `index.ts` exports all 16+ component names including Sheet, Avatar, Popover, Breadcrumb
6. `npm run test` passes all component tests including the batch axe scan
7. Batch axe scan finds 0 critical/serious violations
8. `@/` path alias resolves correctly in build output (no unresolved import errors)
</verification>

<success_criteria>
- All 16+ shadcn/ui components vendored into `frontend/src/components/ui/`
- Badge renders 4 status variants (open/resolved/duplicate/bogus) using CSS token colors
- Button has 2px solid primary focus ring with 2px offset
- Input uses CSS token `bg-background` for dark mode support
- Barrel export `index.ts` re-exports all component primitives
- Component tests pass: variant rendering, focus ring, dialog open/close, axe scan
- Batch axe scan: 0 critical/serious violations across 12 core components
- Build succeeds with `@/` alias resolving correctly
</success_criteria>

<output>
After completion, create `.planning/phases/07-react-design-system-and-shell/07-02-SUMMARY.md` following the summary template.
</output>
